<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingStart="16dp"
    android:paddingEnd="16dp"
    android:paddingTop="6dp"
    android:paddingBottom="6dp">

    <!-- AI Source Label (Hidden by default, shown only for bot messages) -->
    <TextView
        android:id="@+id/textSource"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="11sp"
        android:textColor="#888888"
        android:textStyle="bold"
        android:visibility="gone"
        android:layout_marginBottom="2dp"
        android:paddingStart="4dp"
        android:paddingEnd="4dp"
        android:text="Gemini AI" />

    <!--
        The message bubble.
        MessageAdapter applies backgroundTint to bg_chat_bubble.
        Gravity is set to END (user) or START (bot).
    -->
    <TextView
        android:id="@+id/textMessage"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:maxWidth="280dp"
        android:background="@drawable/bg_chat_bubble"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        android:paddingTop="12dp"
        android:paddingBottom="12dp"
        android:textSize="16sp"
        android:elevation="2dp"
        android:text="Message" />

    <!-- Timestamp -->
    <TextView
        android:id="@+id/textTimestamp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:paddingTop="4dp"
        android:paddingStart="4dp"
        android:paddingEnd="4dp"
        android:textSize="12sp"
        android:textColor="#BBFFFFFF"
        android:text="12:00 PM" />

</LinearLayout>
